version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:80"
    environment:
      BOT_TOKEN: "your_test_token"
      BOT_USERNAME: "your_test_bot"
      WEBHOOK_SECRET: "your_secret"
    volumes:
      - .:/var/www/html
